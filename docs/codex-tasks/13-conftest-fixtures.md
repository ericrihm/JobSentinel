# Task: Already Completed

Shared test fixtures have been built in tests/conftest.py:
- sample_job() — realistic Google SWE posting
- scam_job() — obvious scam with multiple red flags
- ghost_job() — stale 60-day reposted job
- temp_db() — SentinelDB with temp path, auto-cleanup
- seeded_db() — temp_db with seed_default_patterns()

No further action needed.
